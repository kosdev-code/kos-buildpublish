This directory contains the working directory for handling secrets.  

The contents in this directory are very sensitive and should not be disclosed to others or checked into a git repository.

Inside this directory, there are several files/directories of note:

*.7z       : These are the encrypted secrets files that have been saved with the make_secrets_file.sh
developer/ : This is the developer directory which is local to the current user of this machine.  It should not be passed to others.
*/         : all other directories are the working directories for a secrets file for a particular organization.

